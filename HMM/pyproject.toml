[project]
name = "one-direction-hmm"
version = "0.1.0"
description = "Classical HMM/Viterbi baseline for One-Direction map matching"
requires-python = ">=3.10"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
